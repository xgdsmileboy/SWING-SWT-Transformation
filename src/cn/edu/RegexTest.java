package cn.edu;

public class RegexTest {

	public static void main(String[] args) {
		// String regex = "\\s*([a-zA-Z_]\\w*\\.)*[a-zA-Z_]\\w*\\(.*\\)";
		// String str = " jf.setSize(200,100)";
		//
		// Pattern p = Pattern.compile(regex);
		// Matcher m = p.matcher(str);
		// System.out.println(m.matches());

		// String s =
		// "dsadsadas<peter>dsadasdas<lionel>\"www.163.com\"<kenny><>";
		// Pattern p = Pattern.compile("(<[^>]*>)");
		// Matcher m = p.matcher(s);
		// List<String> result=new ArrayList<String>();
		// while(m.find()){
		// result.add(m.group());
		// }
		// for(String s1:result){
		// System.out.println(s1);
		// }

		// String s = "JButton b = new JButton();";
		// String r = s.replaceAll("[]", replacement)

		// File headPath = new File("D:\\a.txt");//获取文件夹路径
		// FileWriter fw = null;
		// try {
		// fw = new FileWriter(headPath, true);
		// } catch (IOException e) {
		// e.printStackTrace();
		// }
		//
		// try {
		//
		// fw.write("hello");
		// fw.write("\n");
		// fw.write("world");
		// fw.flush();
		// fw.close();
		// } catch (IOException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

	}
}
